import AppLayouts from "@/components/appLayouts"
import { Text } from "react-native"

export default function Home() {
    return(           
        <AppLayouts heading='Home'>
            <Text>Ini Home</Text>
        </AppLayouts>
    )
}